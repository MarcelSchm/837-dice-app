# gradlew MUST be checked in with LF, otherwise the Linux CI runner fails
# ("bad interpreter"). Windows batch files need CRLF.
* text=auto
gradlew text eol=lf
*.sh text eol=lf
*.bat text eol=crlf
*.jar binary
*.png binary
*.jpeg binary
*.jpg binary
